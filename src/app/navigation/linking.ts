const linking = {
  prefixes: ['taskcraft://'],
  config: {
    screens: {
      GoogleCallback: 'auth/google/callback',
      ResetPasswordForm: 'auth/reset-password',
    },
  },
};

export default linking;
