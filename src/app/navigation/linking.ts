const linking = {
  prefixes: ['taskcraft://'],

  config: {
    screens: {
      GoogleCallback: 'auth/google/callback',
      Home: 'home',
      Login: 'login',
      Register: 'register',
    },
  },
};

export default linking;
